services:
  - type: web
    name: triage-ai-proxy
    env: node
    rootDir: apps/proxy
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
